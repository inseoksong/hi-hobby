<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Class">

<!-- 클래스 생성 -->
	<insert id="create" parameterType="classVO">
		insert into tbl_class
		(class_nickname, class_title, class_category, class_place, class_price, class_start, class_end, class_approve, class_img, class_one, class_status, user_num, class_introduce)
		values (#{classNickname}, #{classTitle}, #{classCategory}, #{classPlace}, #{classPrice}, #{classStart}, #{classEnd}, #{classApprove}, #{classImg}, #{classOne}, #{classStatus}, #{userNum}, #{classIntroduce})
	</insert>
	
<!-- 클래스 수정 -->
	<update id="modify" parameterType="classVO">
	</update>
	
<!-- 클래스 삭제 -->
	<update id="del" parameterType="_int">
	update tbl_class set class_status = '1' 
	where class_num = '#{classNum}';
	</update>
	
<!-- 지정한 클래스 조회 -->
	<select id="view" parameterType="_int" resultType="classVO">
	select class_num, class_nickname, class_title, class_category, class_place, class_start, class_end, class_approve, class_img, class_one, class_status, class_introduce, user_num
	from tbl_class
	where class_num = '#{classNum}'
	</select>	

<!-- 내가 개설한 클래스 보기 -->
	<select id="viewMine" parameterType="_int" resultType="classVO">
	select class_num, class_nickname, class_title, class_category, class_place, class_start, class_end, class_approve, class_img, class_one, class_status, class_introduce
	from tbl_class
	where user_num = '#{userNum}'
	</select>	
	
<!-- 승인 대기 클래스 보기 -->
	<select id="pending" parameterType="_int" resultType="classVO">
	select class_num, class_nickname, class_title, class_category, class_place, class_start, class_end, class_img, class_one, class_status, class_introduce, user_num
	from tbl_class
	where class_approve = '0'
	</select>
	
<!-- 클래스 승인 -->
	<update id="confirm" parameterType="classVO">
	update tbl_class set class_approve = '1' 
	where class_num = '#{classNum}';
	</update>

<!-- 클래스 목록 조회 -->
	<select id="listView" resultType="classVO">
		select class_num, class_nickname, class_title, class_category, class_place, class_start, class_end, class_img, class_introduce
		from tbl_class <!-- where class_approve = true -->
	</select>

</mapper>