<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Inquiry">


<!-- 문의글 작성-->
	<insert id="write" parameterType="inquiryVO">
	</insert>

<!-- 문의글 수정 -->
	<update id="modify" parameterType="inquiryVO">
	</update>

<!-- 문의글 삭제 -->
	<delete id="del" parameterType="inquiryVO">
	</delete>

<!-- 문의글 불러오기 -->
	<select id="view" parameterType="inquiryVO" resultType="inquiryVO">
	</select>

<!-- 전체 문의 보기 -->
	<select id="list" parameterType="inquiryVO" resultType="inquiryVO">
	</select>






</mapper>