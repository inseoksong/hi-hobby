<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http//mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Coupon">
	<!-- 쿠폰 생성 -->

	<!-- 쿠폰 사용-->

	<!-- 쿠폰 정보 가져오기 -->
	
	<!-- 쿠폰 사용 여부 확인 -->
	<select id="checkCoupon" parameter="string" resultType="int">
	select count(coupon_user)
	from tbl_coupon
	where coupon_user = #{userNum}
	</select>

</mapper>