<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<!-- 이 파일을 통해 데이터베이스에 접근함 -->

<configuration>
	<typeAliases>
		<typeAlias type = "com.member.domain.vo.MemberVO" alias="memverVO"/> <!-- 여기서 알리아스를 줬기때문에, xml 에서 mermberVO를 만나면 이경로를 알게됨 -->
	</typeAliases>
	<!-- 복수형이면 항상 단수형도 있다 -->
	<environments default = "mysql">
		<environment id="mysql">
			<transactionManager type="JDBC"/>
				<dataSource type="POOLED"> <!-- 풀드라는 형식을 쓰겠다 -->
					<property name="driver" value="com.mysql.cj.jdbc.Driver"/>
														<!-- mysql 드라이버로 초기화 하는 과정 -->
					<property name="url" value="jdbc:mysql://localhost:3306/form"/>
					<property name="username" value="root"/>
					<property name="password" value="1234"/>
										<!-- id/pw 입력 -->
				</dataSource>
		</environment> 
	</environments>
	
<mappers>
	<mapper resource = "com.mybatis.mapper/MemberMapper.xml"/>
</mappers>

</configuration>